{
  "name": "@mui-poc/mui",
  "version": "1.0.0",
  "main": ".dist/index.js",
  "scripts": {
    "build": "npm run clean && tsc",
    "start": "tsc --watch",
    "clean": "rimraf .dist"
  },
  "author": "Tyler Hall",
  "license": "ISC",
  "engines": {
    "node": "10.16.3"
  },
  "peerDependencies": {
    "react": "^16.8.0",
    "react-dom": "^16.8.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.0"
  }
}
