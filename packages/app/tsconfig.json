
{
  "extends": "../../tsconfig.json",
  "exclude": [
    "node_modules",
    "dist",
    ".next",
    "out",
    "next.config.js",
    "../../cypress" // * has separate TS config
  ],
  "include": [
    "**/*.ts",
    "**/*.tsx",
    "./jest.setup.ts"
  ]
}
